var isHappy = function(n){
    const set = new Set();
    let valid = true;

    while(valid){
        const nums = `${n}`.split("");
        let sum = 0;

        for(let x = 0; x<nums.length;x++){
            sum +=
        }    }
}